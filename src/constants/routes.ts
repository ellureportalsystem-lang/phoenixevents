// List of blocked pages (temporarily unavailable)
export const BLOCKED_PAGES = ["/gallery", "/events", "/services", "/collaborations"];

// Allowed pages
export const ALLOWED_PAGES = ["/", "/contact"];
